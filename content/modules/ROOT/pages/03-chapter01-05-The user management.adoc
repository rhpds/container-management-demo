= The user management

== Demo flow

=== Know
Explain how identity and access workflows reduce security risk and improve auditability in multi-team environments.

=== Show
Demonstrate user, group, and identity-provider operations that enforce least privilege with less administrative friction.

The *User Management* is the OpenShift Container Platform web console interface that allows cluster administrators to manage the users and groups of the cluster. Go to *Home → Security → User Management* to access the user management.

In OpenShift, the User Management section of the web console serves as the administrative bridge between your external corporate directory and the internal Kubernetes RBAC (Role-Based Access Control).
Having these tools embedded directly in the console—rather than relying solely on the CLI—offers several "high-velocity" advantages for cluster administrators.

== Simplified Identity Provider (IdP) onboarding 

Go to `Administration` -> `Cluster Settings` -> `Configuration` -> `OAuth`
In `Identity Providers` click `Add` to show the available options
Instead of manually crafting complex Custom Resource (CR) YAML files, the console provides a wizard-driven interface to connect your cluster to external identity sources like LDAP, GitHub, or Google.

*Advantage*: You can configure, test, and troubleshoot your connection to an IdP (Identity Provider) in minutes. The UI validates your parameters (like URLs and Secret names) before you apply them, reducing the risk of locking yourself out of the cluster.

image::user-management.png[OpenShift Console User Management,1000,1000]

== Powerful user impersonation for debugging

You can click on a specific user in the `User Management` -> `Users` list and select `Impersonate User`. 

*Advantage*: This allows an admin to see exactly what a specific developer sees. If a developer complains they "can't see the monitoring tab," you can impersonate them to verify if your RBAC policies are actually working as intended without needing their password.

== Visual RBAC Mapping

Understanding who has access to what in a large cluster with hundreds of namespaces is notoriously difficult. The OpenShift console provides a clear, searchable table of RoleBindings and ClusterRoleBindings.

*Advantage*: You can instantly see which users are "Cluster Admins" versus "Project Viewers." It also makes it easy to spot "orphaned" permissions—where a user has been deleted from your company's LDAP but their internal OpenShift User object or RoleBinding still exists.

== Group Management & LDAP Sync Visibility

Managing users individually is a nightmare. OpenShift relies on Groups to scale permissions.

*Advantage*: The console allows you to manually create local groups for quick projects or, more importantly, monitor the status of your LDAP Group Sync. If a sync fails, the console's event stream will highlight the specific error (e.g., "invalid credentials" or "search base not found"), allowing for much faster resolution than digging through logs in the openshift-authentication namespace.

== Live Demo: From Zero Access to Scoped Access in 60 Seconds

This walkthrough demonstrates the full RBAC lifecycle — create a user, group them, grant scoped access, and verify it from the user's perspective — entirely from the console. The only CLI command is a single line run in the web terminal from the previous section.

=== Step 1 — Create a user (web terminal)

Open the web terminal and run:

[source,bash]
----
oc create user demouser
----

`demouser` now appears in *User Management → Users* immediately. No password is needed — for the impersonation demo, only the User object has to exist.

=== Step 2 — Impersonate before granting access (the "before" state)

. Navigate to *User Management → Users* and click on `demouser`.
. Click *Impersonate User*.
. The console switches to `demouser`'s view — the project list is empty and most menus are gone.

[TIP]
====
*"This user exists on the cluster but has no permissions. This is the OpenShift default: zero access until explicitly granted. No implicit trust, no open namespaces."*
====

. Click *Stop Impersonation* to return to your admin session.

=== Step 3 — Grant scoped access to one project

The RoleBinding must be created inside the `grafana-demo` project and bound directly to the *user* (not the group) — the console's impersonation feature sends only `Impersonate-User` and does not resolve group memberships, so a group binding is invisible during impersonation.

. In the project dropdown at the top of the console, switch to the `grafana-demo` project.
. Navigate to *User Management → demouser → RoleBindings* and click *Create binding*:
+
[cols="1,2"]
|===
| Field | Value

| Binding type | *Namespace role binding (RoleBinding)*
| Name | `demo-view`
| Namespace | `grafana-demo`
| Role name | `view`
| Subject | *User*
| Subject name | `demouser`
|===
+
. Click *Create*.

[NOTE]
====
*Why User and not Group?* The console's impersonation feature sets `Impersonate-User: demouser` but does not automatically inject group memberships. A group-based RoleBinding only applies when the user authenticates normally through an identity provider (which injects the groups into their token). For the live impersonation demo to show the "after" state, the binding must target the user directly.

In a real deployment you would bind the *group* so that all group members inherit access automatically. After finishing the demo, you can also create a second binding with Subject: *Group* / `demo-viewers` to illustrate that pattern — but the impersonation demo relies on the user binding above.
====

=== Step 4 — Impersonate again (the "after" state)

. Return to *User Management → Users → demouser → Impersonate User*.
. The `grafana-demo` project now appears. Open it — all resources are visible but all edit and delete actions are greyed out.
. Click *Stop Impersonation*.

[TIP]
====
*"From zero access to scoped read-only in under a minute, without writing a single YAML file or restarting anything. In a real environment this same pattern scales to hundreds of users through LDAP group sync — the console just surfaces the result."*
====