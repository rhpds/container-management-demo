apiVersion: v1
kind: Secret
metadata:
  name: logging-loki-s3
  namespace: {{ .Values.logging.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "0"
stringData:
  bucketnames: {{ .Values.minio.bucket }}
  endpoint: http://minio.{{ .Values.minio.namespace }}.svc:9000
  access_key_id: {{ .Values.minio.rootUser }}
  access_key_secret: {{ .Values.minio.rootPassword }}
